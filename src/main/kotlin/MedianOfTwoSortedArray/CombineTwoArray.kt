package MedianOfTwoSortedArray

class CombineTwoArray {

    fun combineArray (arr1 : IntArray , arr2 : IntArray) {
        val mergedArray = arr1 + arr2
    }
    fun middleElement (mergedArray: IntArray){
        val result = {
            for (i in mergedArray.withIndex()){

            }
        }
    }
}